syntax = "proto3";
package terra.smartaccounts.v1;

import "gogoproto/gogo.proto";
import "terra/smartaccounts/v1/params.proto";

option go_package = "github.com/terra-money/core/v2/x/smartaccounts/types";

// GenesisState defines the smartaccounts module's genesis state.
message GenesisState {
  // params defines the paramaters of the module.
  Params params = 1 [ (gogoproto.nullable) = false ];
}
